{
  "name": "it-test-on-animal-api",
  "version": "1.0.0",
  "description": "API to verify if a product or brand makes tests on animals",
  "main": "src/server.js",
  "repository": "https://github.com/douglasffilho/it-tests-on-animal-api",
  "author": "Douglas Filho",
  "license": "MIT",
  "scripts": {
    "debug": "yarn eslintfy && NODE_ENV=dev DEBUG=app:* node --inspect-brk -r sucrase/register src/server.js",
    "dev": "yarn eslintfy && NODE_ENV=dev DEBUG=app:* nodemon src/server.js",
    "prod": "NODE_ENV=production DEBUG=app:* sucrase-node src/server.js",
    "eslintfy": "yarn eslint --fix src --ext .js",
    "test": "NODE_ENV=test DEBUG=app:* jest",
    "clean": "sh ./clean.sh"
  },
  "dependencies": {
    "config": "^3.3.6",
    "express": "^4.17.1",
    "sucrase": "^3.18.1"
  },
  "devDependencies": {
    "debug": "^4.3.1",
    "eslint": "^7.25.0",
    "jest": "^26.6.3",
    "nodemon": "^2.0.7"
  }
}
